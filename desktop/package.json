{
  "name": "vocavid-desktop",
  "private": true,
  "version": "1.1.1",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "prepare": "powershell -ExecutionPolicy Bypass -File scripts/prepare-payload.ps1",
    "build:installer": "powershell -ExecutionPolicy Bypass -File scripts/build-installer.ps1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.0"
  }
}
